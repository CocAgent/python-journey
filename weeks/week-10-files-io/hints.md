# Hints — Week 10

- Build paths with `Path("folder") / "file.txt"`.
- Use `path.parent.mkdir(parents=True, exist_ok=True)`.
- CSV writers need `newline=""`.
- JSON keys must be strings; values must be JSON-compatible.
- Load a saved replay and validate its labels before trusting its contents.
