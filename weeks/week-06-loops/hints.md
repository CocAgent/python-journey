# Hints — Week 06

- `range(1, 6)` produces 1 through 5.
- Start learner-facing numbering with `enumerate(values, start=1)`.
- Use `zip(left, right, strict=True)` when lengths should match.
- Update a `while` condition variable on every relevant path.
- Start a comprehension with the output expression, then its `for` clause.
